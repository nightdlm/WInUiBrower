<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WInUiBrower.Views.ServerConfig"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WInUiBrower.Views"
    xmlns:component="using:WInUiBrower.Controller"
    xmlns:model="using:WInUiBrower.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 顶部工具栏 -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="新增"/>
        </CommandBar>
        <!-- 动态生成的控件列表 -->
        <ListView Grid.Row="1" 
          ItemsSource="{x:Bind model:DynamicContants.Instance.Items,Mode=TwoWay}" 
          AllowDrop="True"
          CanReorderItems="True"
          CanDragItems="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <component:ConfigInfo Margin="0,0,0,0"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</Page>
