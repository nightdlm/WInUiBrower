<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WInUiBrower.Views.StatusMonitorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WInUiBrower.Views"
    xmlns:model="using:WInUiBrower.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=model:DynamicContants}"
    >

    <Grid>
        <ListView x:Name="ServicesListView" Margin="10" ItemsSource="{Binding Items}">
            <ListView.Header>
                <Grid Padding="10" Background="{ThemeResource SystemBaseLowColor }">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="服务名" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="端口" FontWeight="Bold" />
                    <TextBlock Grid.Column="2" Text="状态" FontWeight="Bold" />
                    <TextBlock Grid.Column="3" Text="操作" FontWeight="Bold" />
                   
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <!-- 服务名 -->
                        <TextBlock Grid.Column="0" Text="{Binding Key}" VerticalAlignment="Center" />

                        <!-- 端口 -->
                        <TextBlock Grid.Column="1" Text="{Binding Port}" VerticalAlignment="Center" />

                        <!-- 状态 -->
                        <TextBlock Grid.Column="2" Text="{Binding Status}" VerticalAlignment="Center" />

                        <!-- 操作按钮 -->
                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                            <Button Content="启动" Margin="0,0,5,0" />
                            <Button Content="重启" Margin="0,0,5,0"/>
                            <Button Content="停止"/>
                        </StackPanel>
                        
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
